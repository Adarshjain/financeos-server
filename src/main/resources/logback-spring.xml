<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <property name="LOG_FILE" value="logs/financeos.log"/>
    <property name="LOG_ARCHIVE" value="logs/archive/financeos-%d{yyyy-MM-dd}.%i.log.gz"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Rolling File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover with compression -->
            <fileNamePattern>${LOG_ARCHIVE}</fileNamePattern>
            <!-- Max file size before rotation -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Keep 7 days of history -->
            <maxHistory>7</maxHistory>
            <!-- Total size cap for all logs -->
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Logging Levels -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Application Specific Logging -->
    <logger name="com.financeos" level="DEBUG"/>
    <logger name="org.springframework.security" level="DEBUG"/>
    <logger name="org.hibernate.SQL" level="ERROR"/>
    <logger name="org.hibernate.orm.jdbc.bind" level="ERROR"/>
    <!-- PDFBox/FontBox emit unactionable font-decode warnings and stack traces per
         statement (broken embedded fonts, system-font-cache scans); silence them. -->
    <logger name="org.apache.pdfbox" level="OFF"/>
    <logger name="org.apache.fontbox" level="OFF"/>
</configuration>
